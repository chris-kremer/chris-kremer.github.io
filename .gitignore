_site
.sass-cache
.jekyll-cache
.jekyll-metadata
vendor

.DS_Store

# Local admin tool — keep off GitHub
admin.py
admin/__pycache__/

# AI project (not ready)
ai_project/

# Raw behavior-bench CSVs (processed into assets/data/behavior-bench.json)
behavior-bench/
prepare_bench_data.py

# Raw Notion export (already imported)
Notion/
